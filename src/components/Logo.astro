---
---

<p class="remote-valley-logo">Remote Valley<span class="os"> OS</span></p>

<style>
	.remote-valley-logo {
		width: fit-content;
		margin: 0;
		font-family: var(--theme-font-family-display);
		font-size: var(--font-size-md);
		color: inherit;
		font-weight: 700;
	}
	.remote-valley-logo:hover {
		text-decoration: underline;
		cursor: pointer;
	}
	.os {
		color: var(--accent-glacial-blue);
		font-family: var(--theme-font-family-sans);
		font-weight: 300;
		font-size: 0.85em;
	}
</style>
