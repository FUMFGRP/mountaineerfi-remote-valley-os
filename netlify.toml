[build]
  command = "npm run build"
  publish = "dist/"

[build.environment]
  NODE_VERSION = "20"

# Netlify Forms: forms are detected from the static HTML at deploy time.
# Each form uses data-netlify="true" and a hidden form-name input.
